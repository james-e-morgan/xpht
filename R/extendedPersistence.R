extendedPersistence <- function(bdryCurves,
                                imgName,
                                directions,
                                tolerance = 1/sqrt(2),
                                saveOutput = FALSE,
                                outputDir = NULL,
                                fName = NULL,
                                verbose = TRUE) {
    
}